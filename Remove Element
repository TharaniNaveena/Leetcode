class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        count = 0
        n = len(nums)
        for i in range(0,len(nums)):
            if(nums[i] != val):
                count +=1 
            else:
                nums[i] = 0
        nums.sort(reverse=True)
        return count
